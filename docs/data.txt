Add UI pages for managing workflows and signature requests.
Hook onboarding/offboarding tasks to employees (filter by employee_id).
Enforce auth with your existing authenticate, authorize middleware when ready.
Integrate a real e-sign provider if desired (Docusign, HelloSign, etc.).


For Selection UX (employees/documents) in signature requests, I kept a minimal ID input. If you want a combobox selector like the one used in Contract/Document forms, I can add it here too.
If you want these pages discoverable from the main HR nav, I can add “Workflows” and “Signatures” entries to HrNavigation; currently they’re accessible via direct routes.
For Workflows, we can later add a visual step editor (drag-and-drop) and role-based visibility for steps.
For Signatures, integrating with a real provider can be done by extending the signatures API to call upstream providers and handling callbacks.
